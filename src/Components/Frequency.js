import React, {Component} from 'react';
import {getUser} from '../Modules/User';


class AddFrequency extends Component {
  constructor(props) {
     super(props);
     this.state = {
         selectedPlant: this.state.plant
     };
   }
}
